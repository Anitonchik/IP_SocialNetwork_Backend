spring:
  main:
    banner-mode: off
  application:
    name: IP_SocialNetwork_Backend
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  liquibase:
    enabled: true
    drop-first: true
    change-log: classpath:db/master.yml
server:
  port: 8000
